name: Keep Streamlit Awake

on:
  schedule:
    - cron: "0 */10 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Curl request
        run: curl -I https://https://gw2-tools.streamlit.app/